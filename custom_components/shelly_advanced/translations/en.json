{
  "config": {
    "step": {
      "user": {
        "title": "Shelly Advanced",
        "description": "Keep a roaming Shelly's config entry in sync with wherever it is reachable — directly on the main network, or behind another Shelly acting as a WiFi range extender.",
        "menu_options": {
          "manual": "Add a single Shelly",
          "discover": "Discover and add all Shellies"
        }
      },
      "manual": {
        "title": "Add a single Shelly",
        "description": "Pick the roaming Shelly to keep in sync. The extender is optional — leave it blank to auto-detect which Shelly is currently extending this device (all other Shellies are scanned when it is not directly reachable).",
        "data": {
          "client_entry_id": "Client Shelly (the device to keep in sync)",
          "client_direct_host": "Client direct host (mDNS name reachable on the main network, e.g. shelly-xxx.lan)",
          "extender_host": "Extender host (optional — leave blank to auto-detect the extender, or pin a specific one e.g. shelly-yyy.lan)"
        }
      },
      "discover": {
        "title": "Discover Shellies",
        "description": "Select the Shelly devices to follow. Each one's direct host defaults to its current address. Leave the extender blank to auto-detect it per device (recommended); or pin a shared extender host for all selected devices. You can fine-tune any of them afterwards.",
        "data": {
          "selected_clients": "Shelly devices to add",
          "extender_host": "Shared extender host (optional — blank = auto-detect)"
        }
      },
      "discovery_confirm": {
        "title": "Shelly Advanced",
        "description": "Follow **{name}** with Shelly Advanced? The extender is auto-detected — optionally pin a specific one below.",
        "data": {
          "extender_host": "Extender host (optional — blank = auto-detect)"
        }
      }
    },
    "flow_title": "{name}",
    "error": {
      "invalid_client": "That config entry is not a usable Shelly device (no MAC / unique id).",
      "cannot_connect_extender": "Could not reach the extender Shelly's WiFi.ListAPClients endpoint."
    },
    "abort": {
      "already_configured": "This client Shelly is already being followed.",
      "no_new_shellies": "All Shelly devices are already being followed.",
      "no_mac": "Could not determine the Shelly's MAC address from discovery.",
      "no_shelly_entry": "This Shelly is not set up in the Shelly integration yet, so there is nothing to follow."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Shelly Advanced options",
        "data": {
          "scan_interval": "Poll interval (seconds)",
          "direct_port": "Direct HTTP port (usually 80)"
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "link": {
        "name": "Network path",
        "state": {
          "direct": "Direct",
          "extender": "Via extender",
          "unreachable": "Unreachable"
        }
      }
    },
    "switch": {
      "follow_enabled": {
        "name": "Auto-reconfigure"
      },
      "eco_mode": {
        "name": "Eco mode"
      },
      "ap_enabled": {
        "name": "Access point"
      },
      "range_extender": {
        "name": "Range extender"
      }
    }
  }
}
